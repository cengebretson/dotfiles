prefix_rule(pattern=["cp"], decision="allow")
prefix_rule(pattern=["ssh-add", "-l"], decision="allow")
prefix_rule(pattern=["codex", "plugin", "add"], decision="allow")
prefix_rule(pattern=["node", "/Users/chris/.codex/plugins/cache/context-mode/context-mode/1.0.162/cli.bundle.mjs"], decision="allow")
prefix_rule(pattern=["node", "/Users/chris/.config/codex/skills/.system/openai-docs/scripts/fetch-codex-manual.mjs"], decision="allow")
prefix_rule(pattern=["git", "--git-dir=/Users/chris/.dotfiles", "--work-tree=/Users/chris"], decision="allow")
prefix_rule(pattern=["git", "-C", "/Users/chris", "--git-dir=/Users/chris/.dotfiles", "--work-tree=/Users/chris", "add", "--", ".config/codex/.gitignore", ".config/codex/AGENTS.md", ".config/codex/hooks.json", ".config/codex/rules/default.rules", ".config/codex/skills/health-check/SKILL.md", ".config/codex/skills/health-check/agents/openai.yaml"], decision="allow")
prefix_rule(pattern=["git", "-C", "/Users/chris", "--git-dir=/Users/chris/.dotfiles", "--work-tree=/Users/chris", "add", "--"], decision="allow")
prefix_rule(pattern=["node", "-e", "const fs=require('fs'); const p='/Users/chris/.claude/settings.json'; const data=JSON.parse(fs.readFileSync(p,'utf8')); if(data.hooks) delete data.hooks.SessionStart; fs.writeFileSync(p, JSON.stringify(data,null,2)+'\\n');"], decision="allow")
prefix_rule(pattern=["git", "add", "--", "CHANGELOG.md", "CLAUDE.md", "README.md", "functions/_coral_cached_pr_row.fish", "functions/_coral_list.fish", "functions/_coral_rebase.fish", "functions/_coral_read_pr_cache.fish", "functions/_coral_write_pr_cache.fish", "tests/branch-list.test.fish", "tests/check.sh", "tests/rebase.test.fish"], decision="allow")
prefix_rule(pattern=["/Users/chris/.local/bin/git-release"], decision="allow")
prefix_rule(pattern=["/bin/zsh", "-lc", "node -e \"const fs=require('fs'); const p='/Users/chris/.claude/settings.json'; const data=JSON.parse(fs.readFileSync(p,'utf8')); data.hooks ||= {}; data.hooks.SessionStart = [{ hooks: [{ type: 'command', command: '\\\"$HOME/.config/claude/hooks/context-mode-cache-heal.mjs\\\" >/dev/null 2>&1' }] }]; fs.writeFileSync(p, JSON.stringify(data,null,2)+'\\n');\""], decision="allow")
prefix_rule(pattern=["node", "-e", "const fs=require(\"fs\"); const p=\"/Users/chris/.claude/settings.json\"; const data=JSON.parse(fs.readFileSync(p,\"utf8\")); data.hooks ||= {}; data.hooks.SessionStart = [{ hooks: [{ type: \"command\", command: \"\\\"$HOME/.config/claude/hooks/context-mode-cache-heal.mjs\\\" >/dev/null 2>&1\" }] }]; fs.writeFileSync(p, JSON.stringify(data,null,2)+\"\\n\");"], decision="allow")
prefix_rule(pattern=["tests/check.sh"], decision="allow")
prefix_rule(pattern=["git", "add", "--", "CHANGELOG.md", "README.md", "scripts/clear-after-delay", "scripts/tmux-attention", "tests/check.sh"], decision="allow")
prefix_rule(pattern=["git", "push", "origin"], decision="allow")
prefix_rule(pattern=["make", "test"], decision="allow")
prefix_rule(pattern=["git", "-C", "/Users/chris/Desktop/projects/tmux/tmux-fzf-jump", "add", "--"], decision="allow")
prefix_rule(pattern=["git", "-C", "/Users/chris/Desktop/projects/tmux/tmux-fzf-jump", "commit", "-m"], decision="allow")
prefix_rule(pattern=["git", "-C", "/Users/chris/Desktop/projects/tmux/tmux-fzf-jump", "push", "origin"], decision="allow")
prefix_rule(pattern=["git", "add", "--"], decision="allow")
prefix_rule(pattern=["git", "commit", "-m"], decision="allow")
prefix_rule(pattern=["make", "lint"], decision="allow")
prefix_rule(pattern=["/bin/zsh", "-lc", "GIT_RELEASE_TEST_CMD='MISE_CACHE_DIR=/private/tmp/mise-cache GOCACHE=/private/tmp/go-build-cache go test ./...' /Users/chris/.local/bin/git-release patch --push"], decision="allow")
prefix_rule(pattern=["git", "-C", "/Users/chris/Desktop/projects/orc", "commit", "-m"], decision="allow")
prefix_rule(pattern=["/bin/zsh", "-lc", "cd /Users/chris/Desktop/projects/orc && GIT_RELEASE_TEST_CMD='MISE_CACHE_DIR=/private/tmp/mise-cache GOCACHE=/private/tmp/go-build-cache go test ./...' /Users/chris/.local/bin/git-release minor --push"], decision="allow")
prefix_rule(pattern=["git", "-C", "/Users/chris/Desktop/projects/orc", "add", "--"], decision="allow")
prefix_rule(pattern=["git", "-C", "/Users/chris", "--git-dir=/Users/chris/.dotfiles", "--work-tree=/Users/chris", "diff"], decision="allow")
prefix_rule(pattern=["git", "-C", "/Users/chris", "--git-dir=/Users/chris/.dotfiles", "--work-tree=/Users/chris", "status"], decision="allow")
prefix_rule(pattern=["git", "-C", "/Users/chris", "--git-dir=/Users/chris/.dotfiles", "--work-tree=/Users/chris", "add", "-v", "--", ".config/codex/AGENTS.md"], decision="allow")
prefix_rule(pattern=["perl", "-0pi", "-e", "s#- If sandboxed commands warn that they cannot write under `~/Library/Caches` for `mise` or Go build cache, rerun with temp cache dirs such as `MISE_CACHE_DIR=/private/tmp/mise-cache GOCACHE=/private/tmp/go-build-cache` and do not treat the warning as a project failure\\.#- If sandboxed commands warn that they cannot write under `~/Library/Caches` for `mise` or Go build cache, prefer adding or using a project-local ignored cache path such as `.cache/mise` and `.cache/go-build` through the repo\\x27s Makefile or test wrapper. For one-off commands, rerun with temp cache dirs such as `MISE_CACHE_DIR=/private/tmp/mise-cache GOCACHE=/private/tmp/go-build-cache`. Do not treat cache permission warnings as project failures.#", "/Users/chris/.config/codex/AGENTS.md"], decision="allow")
prefix_rule(pattern=["perl", "-0pi", "-e", "s#Otherwise: `ctx_batch_execute\\(commands, queries\\)` or `ctx_execute\\(language: \"shell\", code: \"\\.\\.\\.\"\\)`\\.#Otherwise: prefer `ctx_execute(language: \"shell\", code: \"...\")` with aggressive filtering. Use `ctx_batch_execute(commands, queries)` only when several related large outputs must be gathered and searched together.#; s#0\\. \\*\\*MEMORY\\*\\*: `ctx_search\\(sort: \"timeline\"\\)` — after resume, check prior context before asking user\\.\\n1\\. \\*\\*GATHER\\*\\*: `ctx_batch_execute\\(commands, queries\\)` — runs all commands, auto-indexes, returns search\\. ONE call replaces 30\\+\\. Each command: `\\{label: \"header\", command: \"\\.\\.\\.\"\\}`\\.\\n2\\. \\*\\*FOLLOW-UP\\*\\*: `ctx_search\\(queries: \\[\"q1\", \"q2\", \\.\\.\\.\\]\\)` — all questions as array, ONE call \\(default relevance mode\\)\\.\\n3\\. \\*\\*PROCESSING\\*\\*: `ctx_execute\\(language, code\\)` \\| `ctx_execute_file\\(path, language, code\\)` — sandbox, only stdout enters context\\.#0. **MEMORY**: `ctx_search(sort: \"timeline\")` — after resume, check prior context before asking user.\\n1. **SHORT OBSERVATION**: for small fixed outputs (`git status --short`, `git log -1`, short `sed`, targeted `rg`), use normal shell tools instead of context-mode.\\n2. **PROCESSING**: `ctx_execute(language, code)` | `ctx_execute_file(path, language, code)` — derive a terse answer; only stdout enters context. Prefer this over `ctx_batch_execute` when a script can filter/summarize.\\n3. **FOLLOW-UP**: `ctx_search(queries: [\"q1\", \"q2\", ...])` — all questions as array, ONE call (default relevance mode).\\n4. **BATCH GATHER**: `ctx_batch_execute(commands, queries)` — reserve for several related commands whose outputs may be large and need indexing/search in one round trip. Keep commands and queries narrow; avoid broad readbacks.#", "/Users/chris/.config/codex/AGENTS.md"], decision="allow")
prefix_rule(pattern=["perl", "-0pi", "-e", "s#Shell ONLY for: `git`, `mkdir`, `rm`, `mv`, `cd`, `ls`, `npm install`, `pip install`\\.\\nOtherwise: `ctx_batch_execute\\(commands, queries\\)` or `ctx_execute\\(language: \"shell\", code: \"\\.\\.\\.\"\\)`#Shell is fine for short fixed observations and state-changing repo commands (`git`, `mkdir`, `rm`, `mv`, `cd`, `ls`, `npm install`, `pip install`).\\nFor commands that may exceed 20 lines, prefer `ctx_execute(language: \"shell\", code: \"...\")` with aggressive filtering. Use `ctx_batch_execute(commands, queries)` only when several related large outputs must be gathered and searched together.#; s#4\\. \\*\\*WEB\\*\\*#5. **WEB**#; s#5\\. \\*\\*INDEX\\*\\*#6. **INDEX**#", "/Users/chris/.config/codex/AGENTS.md"], decision="allow")
prefix_rule(pattern=["perl", "-0pi", "-e", "s#For commands that may exceed 20 lines, prefer `ctx_execute\\(language: \"shell\", code: \"\\.\\.\\.\"\\)` with aggressive filtering\\. Use `ctx_batch_execute\\(commands, queries\\)` only when several related large outputs must be gathered and searched together\\.#For commands that may exceed 20 lines, use context-mode. Prefer `ctx_batch_execute(commands, queries)` when gathering several related context sources or when the output may be useful for follow-up search; keep commands and queries narrow. Use `ctx_execute(language: \"shell\", code: \"...\")` when one command can filter or summarize to a terse answer.#; s#2\\. \\*\\*PROCESSING\\*\\*: `ctx_execute\\(language, code\\)` \\| `ctx_execute_file\\(path, language, code\\)` — derive a terse answer; only stdout enters context\\. Prefer this over `ctx_batch_execute` when a script can filter/summarize\\.\\n3\\. \\*\\*FOLLOW-UP\\*\\*: `ctx_search\\(queries: \\[\"q1\", \"q2\", \\.\\.\\.\"\\]\\)` — all questions as array, ONE call \\(default relevance mode\\)\\.\\n4\\. \\*\\*BATCH GATHER\\*\\*: `ctx_batch_execute\\(commands, queries\\)` — reserve for several related commands whose outputs may be large and need indexing/search in one round trip\\. Keep commands and queries narrow; avoid broad readbacks\\.#2. **BATCH GATHER**: `ctx_batch_execute(commands, queries)` — preferred for several related commands, outputs that may be large, or context worth indexing/searching in one round trip. Keep batches focused; use narrow labels and queries; avoid broad readbacks.\\n3. **PROCESSING**: `ctx_execute(language, code)` | `ctx_execute_file(path, language, code)` — derive a terse answer when one script can filter/count/summarize; only stdout enters context.\\n4. **FOLLOW-UP**: `ctx_search(queries: [\"q1\", \"q2\", ...])` — all questions as array, ONE call (default relevance mode).#", "/Users/chris/.config/codex/AGENTS.md"], decision="allow")
prefix_rule(pattern=["perl", "-0pi", "-e", "s#2\\. \\*\\*PROCESSING\\*\\*: `ctx_execute\\(language, code\\)` \\| `ctx_execute_file\\(path, language, code\\)` — derive a terse answer; only stdout enters context\\. Prefer this over `ctx_batch_execute` when a script can filter/summarize\\.\\n3\\. \\*\\*FOLLOW-UP\\*\\*: `ctx_search\\(queries: \\[\"q1\", \"q2\", \\.\\.\\.\\]\\)` — all questions as array, ONE call \\(default relevance mode\\)\\.\\n4\\. \\*\\*BATCH GATHER\\*\\*: `ctx_batch_execute\\(commands, queries\\)` — reserve for several related commands whose outputs may be large and need indexing/search in one round trip\\. Keep commands and queries narrow; avoid broad readbacks\\.#2. **BATCH GATHER**: `ctx_batch_execute(commands, queries)` — preferred for several related commands, outputs that may be large, or context worth indexing/searching in one round trip. Keep batches focused; use narrow labels and queries; avoid broad readbacks.\\n3. **PROCESSING**: `ctx_execute(language, code)` | `ctx_execute_file(path, language, code)` — derive a terse answer when one script can filter/count/summarize; only stdout enters context.\\n4. **FOLLOW-UP**: `ctx_search(queries: [\"q1\", \"q2\", ...])` — all questions as array, ONE call (default relevance mode).#", "/Users/chris/.config/codex/AGENTS.md"], decision="allow")
prefix_rule(pattern=["/bin/zsh", "-lc", "git --git-dir=\"$HOME/.dotfiles\" --work-tree=\"$HOME\" add -- .config/git/.gitignore .config/fish/.gitignore .config/codex/.gitignore .config/claude/.gitignore"], decision="allow")
prefix_rule(pattern=["/bin/zsh", "-lc", "git --git-dir=\"$HOME/.dotfiles\" --work-tree=\"$HOME\" check-ignore -v \"$HOME/.config/git/.gitignore\" \"$HOME/.config/fish/.gitignore\" \"$HOME/.config/codex/.gitignore\" \"$HOME/.config/claude/.gitignore\""], decision="allow")
