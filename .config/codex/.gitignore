# Per-machine account/auth state — never share across machines
auth.json
.credentials.json

# Runtime and session data
sessions/
history.jsonl
log/
logs/
tmp/
.tmp/
cache/
context-mode/
plugins/cache/
plugins/.marketplace-plugin-source-staging/
shell_snapshots/
version.json
*.log
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db
*.db-shm
*.db-wal

# Machine-local Codex state
config.toml
installation_id
.personality_migration
models_cache.json
hooks.json.bak.*
goals_*
logs_*
memories_*
state_*

# Installed system skills are managed by Codex, not personal dotfiles
skills/.system/

# Note: AGENTS.md and config.shared.toml are intentionally trackable.
# Keep secrets out of shared config — use env vars, never hardcoded keys.
