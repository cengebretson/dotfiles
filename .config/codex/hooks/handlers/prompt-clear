#!/usr/bin/env bash
# tmux attention marker: clear the waiting flag when a prompt is submitted.
set -u
script="$HOME/.config/tmux/plugins/tmux-attention/scripts/tmux-attention"
[ -x "$script" ] || exit 100
exec "$script" clear >/dev/null 2>&1
