#!/usr/bin/env bash
# Clear the waiting marker and show this Codex turn's project in tmux.
set -u
script="${TMUX_ATTENTION_CLI:-$HOME/.config/tmux/plugins/tmux-attention/scripts/tmux-attention}"
[ -x "$script" ] || exit 100
exec "$script" turn-start >/dev/null 2>&1
